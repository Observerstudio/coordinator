Feature: <what is true after this lands> (#<issue>)

  Scenario: <one observable outcome, domain words: baskets, book, receipt — never table, column, hook>
    Given <state the operator can recognise>
    When <the action through the REAL door: the screen's mutation, or for a backend-only lane the production entry point the screen calls (router / service door) — never a test-only shortcut>
    Then <one outcome>
